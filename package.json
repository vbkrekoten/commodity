{
  "name": "commodity-exchange-site",
  "private": true,
  "packageManager": "pnpm@10.2.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "e2e": "pnpm --filter @apps/web e2e",
    "db:migrate": "pnpm --filter @apps/api prisma:migrate",
    "db:seed": "pnpm --filter @apps/api prisma:seed"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
